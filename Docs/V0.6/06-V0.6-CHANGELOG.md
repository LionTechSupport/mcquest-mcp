# MCQuest MCP v0.6 — Changelog

> CHANGELOG IS HISTORICAL ONLY. It is NEVER implementation authority.
> See the authority order in `00-V0.6-MASTER.md`. Entry status vocabulary:
> PLANNED / IMPLEMENTED / TESTED / COMPLETED (and DOCUMENTATION ONLY for
> phases without code changes).

---

## 2026-09-14 — Phase 0 baseline audit (DOCUMENTATION ONLY)

Status: **COMPLETED (audit-only; no code or test changes)**

Changes:
- Created the V0.6 control plane (this directory, including
  `prompts/00-BASELINE-AUDIT.md`).
- Inspected the V0.5 documentation set (`Docs/V0.5/*`) WITHOUT altering it.
- Re-opened V0.5 Phases E/F and classified every item
  (`05-V0.6-DECISIONS.md`).
- Measured tool selection, evidence completeness, context efficiency, legacy
  tools, applicability, and security boundaries against the live MCP endpoint
  (evidence: `03-V0.6-STATE.md`).
- Proposed minimal v0.6 scope and Phase 1 objective (PROPOSED, pending user
  authorization).

Tests:
- `uv run pytest` → **229 passed, 1 skipped** (v0.5 baseline re-verified).
- No v0.6 tests authored yet (Phase 0 required none).

Contract deviations:
- None. No v0.5 document, `src/`, or `tests/` file was modified.

Security changes:
- None.

Next:
- Phase 1 requires user authorization (objective in `05-V0.6-DECISIONS.md`
  DEC-012). Do not start Phase 1 automatically.

---

## 2026-09-14 — Phase 1 implementation (authorized scope)

Status: **COMPLETED** (implemented + tested; nothing outside the authorized
scope was changed)

Changes:
- Discoverability hardening (descriptions only; no semantic changes):
  - `find_usages` description now states its TS/TSX/JS/JSX-only scan scope.
  - `find_evidence` description now states the code-stream language set
    (TS/TSX/JS/JSX/CSS), the Markdown docs stream, and the
    `collection_complete` meaning.
  - `phase_context` / `compare_phase` descriptions now state the
    phase-documentation naming convention ("phase-<id>" filename token) and
    the "No documentation found" meaning.
  - Server instructions now clarify that `collection_complete=true` means the
    total/count is known, NOT that every result was delivered.
- `pattern_audit` (V0.5 E1): summary-first default (`[SUMMARY]` +
  `[CATEGORY COUNTS]` + up to 2 samples/category) under the 4,000-char
  presentation budget; additive `category` parameter (single category,
  enum-validated) expanding under the 16,000-char ceiling; legacy `categories`
  selector preserved; canonical deterministic category order.
- Cross-tool acceptance harness (V0.5 F1/F2): new `tests/test_token_sparse.py`
  (59 tests) covering every applicable registered tool — default ≤4000 /
  worst-case ≤16000 raw AND serialized, 200-char line clipping, truthful
  metadata, determinism, read-only behavior.
- Extensions: `tests/test_pattern_audit.py` (12 tests),
  `tests/test_discoverability.py` (6 new assertions).

Tests:
- Focused (pattern_audit + discoverability + token_sparse): **82 passed**.
- Full suite: `uv run pytest` → **305 passed, 1 skipped** (baseline was
  229 passed, 1 skipped; all prior tests remain green).

Live MCP verification (closeout 2026-09-14, implementation commit 5aaaa39
local / 0832ec8 origin-main baseline, `uv run pytest` re-verified
305 passed, 1 skipped during this task):
- Previous stale state: the then-running endpoint served pre-Phase-1 code
  (old pattern_audit dump, no `category` param). RESOLVED by terminating
  only the stale `D:\DeveloperTools\mcquest-mcp` stdio MCP process and
  restarting the configured Cline MCP server (fresh process tree serves
  the current editable-install source).
- T1 pattern_audit default — PASS (live `[SUMMARY]` + `[CATEGORY COUNTS]`
  + `[END]`, budget=4000/16000, no legacy dump).
- T2 pattern_audit category — PASS (live schema exposes additive
  `category`; `category="nowrap"` honored; `category="bogus"` rejected
  with the enum error).
- T3 find_usages description — PASS (live `tools/list` states
  TS/TSX/JS/JSX-only scan scope and total-0-in-other-languages note).
- T4 find_evidence description — PASS (live `tools/list` states code
  stream .ts/.tsx/.js/.jsx/.css, Markdown docs stream, and
  `collection_complete`=total-known-not-delivered with `next_offset`
  continuation).
- T5 phase_context / compare_phase — PASS (live `tools/list` states the
  `phase-<id>` naming convention and "No documentation found" meaning;
  live calls behave accordingly).
- T6 pathological pattern_audit — PASS (representative worst-case
  `category="overflow-x", max_results_per_category=500` bounded,
  budget=16000/16000, metadata truthful, `[END]`, no legacy dump).
- No src/ or tests/ changes were made during this closeout task.

PHASE 1 LIVE VERIFICATION: PASS. PHASE 1: CLOSED. PHASE 2: NOT STARTED.

Contract deviations:
- None. Diagnostics default changes were NOT applied (not authorized).

Security changes:
- None. Read-only behavior and security boundaries unchanged (suite verifies).

Next:
- None — V0.6 is CLOSED. Do not start Phase 2.

---

## 2026-09-14 — V0.6 final release closeout (DOCUMENTATION ONLY)

Status: **V0.6 CLOSED**

- Phase 0: COMPLETE (audit-only; no code or test changes).
- Phase 1: COMPLETE (implemented + tested; full suite **305 passed,
  1 skipped**; live verification **T1–T6 PASS**).
- Phase 2: NOT STARTED / NOT REQUIRED (necessity assessment OPTION A —
  CLOSE V0.6).
- V0.6 Release Gate: **PASS**.
- Final implementation commit: **ecd905c** (= origin/main).
- V0.5: CLOSED and untouched by V0.6.
- Deferred (non-blocking, future-release candidates only — NOT Phase 2):
  diagnostics default tightening (E2, DEFER);
  project/git/phase/compare summary headers (E3, RETIRE);
  all DEC-011 excluded surfaces.

Tests:
- No tests added or changed during closeout (full suite remains
  **305 passed, 1 skipped** per the recorded Phase 1 evidence).

Contract deviations:
- None.

Security changes:
- None.

Stale-banner reconciliation (status wording only; no history, evidence,
decisions, or contract changed):
- `00-V0.6-MASTER.md` (CLOSED banner, final HEAD, live T1–T6 PASS,
  contract-doc wording).
- `02-V0.6-PLAN.md` (CLOSED banner, Phase 1 live-verified, Phase 2
  NOT REQUIRED).
- `03-V0.6-STATE.md` (CLOSED status, committed-scope wording, closure
  section).
- `04-V0.6-TEST-PLAN.md` (CLOSED banner; Phase 2 added no tests).
- `05-V0.6-DECISIONS.md` (CLOSED banner; live T1–T6 PASS wording;
  historical record otherwise preserved).