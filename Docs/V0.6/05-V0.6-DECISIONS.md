# MCQuest MCP v0.6 — Decisions

> STATUS: **V0.6 CLOSED — PHASE 0 RECORD + PHASE 1 IMPLEMENTED + PHASE 2 ASSESSED NOT REQUIRED**
> Status vocabulary: APPROVED = controlling now; PROPOSED = recommendation,
> requires user authorization before implementation; RECORDED = baseline fact
> or classification, no implementation implied. Historical Phase 0 wording
> below is preserved as the baseline record; closure truth lives in STATE /
> CHANGELOG.

---

## DEC-001 — Phase 0 is audit-only

Status: APPROVED

Decision:
V0.6 Phase 0 executes the baseline audit and creates the control plane.
No `src/` or `tests/` modification. No v0.6 implementation phase is marked
complete. Do not redesign MCQuest during Phase 0.

---

## DEC-002 through DEC-009 — V0.5 E/F re-opening classification

Status: RECORDED (evidence below each item)

Authority: `Docs/V0.5/02-V0.5-IMPLEMENTATION-PLAN.md` phases E/F;
`Docs/V0.5/04-V0.5-TEST-PLAN.md` row C; repository reality at `0832ec8` +
docs-only `5aaaa39`.

### E1 — pattern_audit summary-first (counts + samples; additive `category` param; enum validated)

Classification: **CARRY FORWARD TO V0.6** (proposal).

Evidence: `src/mcquest_mcp/tools/audit.py` still defaults `categories="all"`,
emits all 11 categories as plain text, no `[SUMMARY]`, no continuation
metadata; live call confirmed. V0.5 Phase E never ran (STATE overlaps: E =
NOT STARTED). Worst-case output is unbounded (bounded only by the 16k ceiling).

### E2 — diagnostics defaults 20→15 / 12→5 + aggregate budget

Classification: **DEFER**.

Evidence: `src/mcquest_mcp/tools/diagnostics.py` still has
`DEFAULT_MAX_DIAGNOSTICS=20`, `DEFAULT_CONTEXT_LINES=12`; `truncate()` already
enforces the 16k ceiling and "shown (of N)" gives completeness signal.
Default-change is a minor token-efficiency gain, not required for v0.6
reliability.

### E3 — project/git/phase/compare summary headers

Classification: **RETIRE / SUPERSEDE**.

Evidence: `project_info`, `project_context`, `git_context`, `compare_phase`
are already compact, deterministic, plain-text, ceiling-bounded, provably
small without needless pagination. A formal header adds no material agent
value. `compare_phase`'s real limitation is phase-naming/path discoverability
(see the phase/compare findings in `03-V0.6-STATE.md` and DEC-010 item 1),
not a missing header.

### E4 — find_evidence shared page budget (test-plan row R)

Classification: **ALREADY SATISFIED BY V0.5**.

Evidence: `src/mcquest_mcp/tools/context.py` find_evidence merges a
code-then-docs stream under ONE shared page budget with per-scope counts and
total = code + docs (never `min`); live call `counts: code=0; docs=22,
total=22`.

### F1 — test_token_sparse.py acceptance suite (worst-case + serialized invariant)

Classification: **CARRY FORWARD TO V0.6** (proposal).

Evidence: no `tests/test_token_sparse.py` exists (`find_files "token_sparse"`
→ 0). V0.5 Phase F never ran.

### F2 — default ≤4000 / worst-case ≤16000 raw AND json-serialized across every tool

Classification: **CARRY FORWARD TO V0.6** (proposal).

Evidence: individual Phase A–D tests prove bounds per tool, but no single
acceptance harness proves the "every tool" + serialized invariant that F2
specifies. This is the measurable backbone for the v0.6 central question.

### F3 — all 17 tools registered

Classification: **ALREADY SATISFIED BY V0.5**.

Evidence: `tests/test_registration.py` green in suite (229 passed).

### F4 — security/read-only regression + full suite green; STATE marks A–F complete

Classification: **ALREADY SATISFIED BY V0.5** (for the A–D portion);
the "A–F complete" STATE marking is superseded by V0.5 CLOSED with E–F NOT
STARTED as the truthful record.

Evidence: `tests/test_security_readonly.py` green; `uv run pytest` → 229
passed, 1 skipped (2026-09-14).

---

## DEC-010 — V0.6 Phase 1 scope

Status: **APPROVED** (by the 2026-09-14 Phase 1 authorization)

Implemented and verified in Phase 1 (full suite **305 passed, 1 skipped**;
focused 82 passed):

1. Tool-description / discoverability hardening:
   - `find_usages` description states the TS/TSX/JS/JSX-only scan scope
     (and that symbols used only in other languages report total: 0).
   - `find_evidence` description states the code-stream language set
     (TS/TSX/JS/JSX/CSS), the Markdown docs stream, and that
     `collection_complete=true` means the total is known, not delivered.
   - `phase_context` / `compare_phase` descriptions state the
     phase-documentation naming convention ("phase-<id>" filename token) and
     the "No documentation found" meaning.
   - Server instructions clarify the `collection_complete` / `has_more` /
     `next_*` semantics.
2. Carry-forward E1: `pattern_audit` summary-first default (category counts +
   small samples) with additive single-category `category` expansion and enum
   validation — implemented.
3. Carry-forward F1/F2: `tests/test_token_sparse.py` worst-case + serialized
   acceptance harness over every applicable tool — implemented.

## DEC-011 — Explicitly NOT recommended for v0.6

Status: RECORDED

- semantic/vector search, embeddings, AI summarization, repository indexing
- execution tools, generated-artifact access
- broad language support (find_imports/usages staying TS/TSX/JS/JSX; no
  new scanner languages), semantic/type diagnostics
- new security surfaces, unrelated refactoring
- V0.5 E2 default change and E3 headers (DEFER / RETIRE above), unless the
  user explicitly prioritizes them

## DEC-012 — Phase 1 objective

Status: **APPROVED and IMPLEMENTED** (2026-09-14).

Objective executed:
Bring every remaining legacy tool and every tool description under the v0.5
bounded/complete-evidence model, backed by the Phase F acceptance harness —
implemented per DEC-010 items 1–3, gated by the 229+1 baseline and
`test_token_sparse.py` acceptance, with no semantic changes to read/search
behavior. Evidence: full suite **305 passed, 1 skipped** (STATE records the
actual command results). Live endpoint: closeout restart resolved the stale
server; verification **T1–T6 PASS** (see STATE / CHANGELOG).