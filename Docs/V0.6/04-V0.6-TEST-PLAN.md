# MCQuest MCP v0.6 — Test Plan

> STATUS: **PHASE 1 IMPLEMENTED — TESTS AUTHORED AND GREEN**
> Phase 1 authored the tests below; they pass (focused 82; full suite 305
> passed, 1 skipped). A test passing is recorded evidence, not a promise.

---

## A. Baseline

V0.5 final suite (verified 2026-09-14): `uv run pytest` → **229 passed, 1
skipped**. This remains the regression baseline every v0.6 change must keep
green.

Current full suite (Phase 1, verified 2026-09-14): **305 passed, 1 skipped**.

Existing applicable suites (from V0.5) that v0.6 must not regress:
`test_output_budget.py`, `test_read_file.py`, `test_read_doc.py`,
`test_list_files.py`, `test_list_docs.py`, `test_find_files.py`,
`test_search.py`, `test_search_docs.py`, `test_find_imports.py`,
`test_find_usages.py`, `test_find_evidence.py`, `test_phase_context.py`,
`test_registration.py`, `test_discoverability.py`,
`test_security_readonly.py`, `test_diagnostics.py`, `test_compare_phase.py`,
`test_cli.py`.

## B. Phase 0 (baseline audit)

- Required tests: none (audit-only).
- Measurement evidence: recorded in `03-V0.6-STATE.md` (tool-selection,
  evidence-completeness, context-efficiency, legacy, applicability, boundary,
  live MCP).

## C. V0.6 tests (authored in Phase 1, all passing)

Carried forward from V0.5 Phase F (see `05-V0.6-DECISIONS.md`):

| Test | Status | Purpose / evidence |
|---|---|---|
| `tests/test_token_sparse.py` (new, 59 tests) | **AUTHORED + PASSING** | Worst-case acceptance: default calls ≤4000 chars where a normal contract exists; worst-case ≤16000 chars raw AND `json.dumps`-serialized across every applicable tool; 200-char line clip; truthful truncation/metadata; determinism; read-only. Pathological fixtures (500-match searches, 20k-char line, 150-files listing, 300-line sources). |
| `tests/test_pattern_audit.py` (new, 12 tests) | **AUTHORED + PASSING** | pattern_audit summary-first default (counts + ≤2 samples), category expansion (additive, enum-validated), budget bounds, truncation metadata, determinism, ignored/traversal boundaries, schema `category` param. |
| `tests/test_discoverability.py` (extended, +6) | **AUTHORED + PASSING** | Pins the Phase 1 wording: find_usages/find_evidence language scope, phase/compare naming convention, pattern_audit summary-first + category, `collection_complete` semantics. |

## D. Gate Rules

- No v0.6 phase is COMPLETE without executable evidence (tests run + result
  recorded + STATE updated), per `00-V0.6-MASTER.md`.
- Any v0.6 change must keep the 229+1 baseline green.