# MCQuest MCP v0.5 — Implementation Changelog

> CHANGELOG IS HISTORICAL ONLY. It is NEVER implementation authority.
> See the authority order in `00-V0.5-MASTER.md`.
> Only repository reality + executable test evidence prove implementation.
>
> Entry status vocabulary (mutually exclusive, last-wins):
> - `PLANNED` — designed/approved, not implemented
> - `IMPLEMENTED` — repository evidence exists
> - `TESTED` — actual test command was run and passed
> - `COMPLETED` — STATE acceptance criteria are satisfied

## 2026-09-09 — Phase A (INCORRECT HISTORICAL ENTRY)

Status: **INCORRECT / SUPERSEDED — DO NOT USE AS EVIDENCE**

This entry was written before any Phase A implementation existed. It claims:

- "Added shared OutputBudget."
- "Added 4000-char normal budget."
- "Added 16000-char hard ceiling."
- "Added 200-char line clipping."
- "test_output_budget.py: PASS"

**None of these statements were true at the time of writing and none are
true as of `19eeb72`.** Repository evidence (verified 2026-09-09 by doc-
control-plane run):

- `src/mcquest_mcp/formatting.py` — no `OutputBudget`; only
  `truncate(text, limit=MAX_OUTPUT_CHARS)` and `numbered_lines()`.
- `src/mcquest_mcp/config.py:75` — `MAX_OUTPUT_CHARS = 80_000`
  (the v0.5 constant would be 16_000).
- `NORMAL_OUTPUT_CHARS` / `LINE_CLIP_CHARS` — do not exist.
- `tests/test_output_budget.py` — does not exist.
- Search for `OutputBudget` across `src/` — zero matches.

The correct record is: **Phase A was NOT IMPLEMENTED / NOT COMPLETE**
on this date. This entry is retained only as a historical record of an
incorrect claim so future sessions can spot it and treat it as evidence
of what must NOT be trusted.

The authoritative current state lives in `03-V0.5-STATE.md`.

---

## 2026-09-09 — Control-plane normalization (DOCUMENTATION ONLY)

Status: COMPLETED (documentation only; verified by doc reads, no code changes)

Changes:
- Corrected this changelog (see above); the earlier "Phase A COMPLETED"
  entry was false and is now marked INCORRECT / SUPERSEDED.
- Created `04-V0.5-TEST-PLAN.md` (was referenced by MASTER but missing).
- Normalized MASTER operating protocol (RESUME / IMPLEMENTATION /
  COMPLETION / INTERRUPTION rules and explicit document-priority order).
- Normalized STATE to a per-phase status ledger with evidence gate.
- Normalized IMPLEMENTATION-PLAN phase template (objective / allowed files /
  prohibited files / tasks / tests / acceptance / exit / dependencies /
  docs updates).
- Recorded approved decisions D001–D007 in DECISIONS.

Tests:
- No tests were run; no tests exist for Phase A yet.
- Existing regression suite was NOT modified.

Contract deviations:
- None — no application code or tests were changed.

Security changes:
- None — no application code was changed.

Next:
- Phase A implementation per `prompts/01-INFRA.md` after the control-plane
  is approved.

---

## 2026-09-09 — Phase A (OutputBudget Infrastructure)

Status: IMPLEMENTED + TESTED (see evidence below; not yet part of any commit)

Changes (repository evidence — `git status` shows exactly these):
- `src/mcquest_mcp/config.py`:
  - `MAX_OUTPUT_CHARS` changed 80_000 -> 16_000 (v0.5 absolute ceiling, D002);
  - added `NORMAL_OUTPUT_CHARS = 4_000` (D001); `LINE_CLIP_CHARS = 200` (D007).
- `src/mcquest_mcp/formatting.py`:
  - added `_clip_line()` (200-char per-line clip, diagnostics precedent);
  - added `OutputBudget` (normal/ceiling/clip, atomic item emission,
    explicit `truncated`/`omitted` state, `add_header()` so the summary
    survives truncation, `finalize()` that never exceeds the ceiling);
  - rerouted `truncate()` and `numbered_lines()` through `OutputBudget`
    (public signatures preserved; `[OUTPUT TRUNCATED: N characters
    omitted]` marker retained);
  - added `summary_block()` canonical `[SUMMARY]`/`[EVIDENCE]` helper.
- `tests/test_output_budget.py` (new): 29 focused tests.

## 2026-09-09 — Phase B (Windowed Reads: read_file / read_doc)

Status: IMPLEMENTED + TESTED (see evidence below; not yet part of any commit)

Changes (repository evidence — `git status` shows exactly these Phase B files):
- `src/mcquest_mcp/formatting.py`:
  - added `read_window_block()` — windowed-read renderer (D004/D010):
    default read = at most 250 lines under the 4,000 normal budget;
    continuation (start_line > 1, no end_line) and explicit ranges use the
    16,000 ceiling; explicit ranges clamped to 1,000 lines; summary-first
    [SUMMARY] block (tool, scope path, start_line, end_line = ACTUAL
    rendered end, total_lines, next_start_line, has_more, truncated,
    budget) emitted via OutputBudget.add_header so it survives truncation;
    exact pre-emission length accounting so finalize() never pops and
    end_line/next_start_line describe the actual page; 200-char per-line
    clip; [END] trailer; truncation marker on budget cuts;
  - added additive `OutputBudget.mark_truncated()` — lets a caller record
    an exact presentation cut (read windows) so finalize() emits the
    marker with the correct omitted count (Phase A surface untouched;
    Phase A tests unchanged and green).
- `src/mcquest_mcp/tools/files.py` — read_file routes through
  read_window_block (validation, 2MB guard, utf-8 read unchanged).
- `src/mcquest_mcp/tools/docs.py` — read_doc routes through
  read_window_block (markdown check, 2MB guard unchanged).
- `src/mcquest_mcp/server.py` — end_line descriptions for
  mcquest_read_file / mcquest_read_doc updated (window semantics);
  no parameter or schema-default change.
- `tests/test_read_file.py` (new, 22 tests),
  `tests/test_read_doc.py` (new, 15 tests).

Tests (executable evidence):
- `uv run pytest tests/test_read_file.py tests/test_read_doc.py -q` -> 37 passed
- Explicit regression run
  `uv run pytest tests/test_security_readonly.py tests/test_registration.py
  tests/test_discoverability.py tests/test_output_budget.py -q` -> 44 passed
- `uv run pytest` -> 119 passed, 1 skipped
  (82 + 1 baseline after Phase A unchanged; +37 new focused tests;
   no existing test was modified or regressed)

Contract verification (actual measurements, not estimates):
- default read of a 5,000-line file: 3,979 raw chars (serialized 4,117)
  <= 4,000 normal budget;
- explicit 1..5000 read: 15,964 raw chars (serialized 16,352) <= 16,000;
- worst compliant (1.6MB) file explicit read: 15,910 raw (serialized
  16,436), truncated: true;
- 20,000-char single line clipped to exactly 200 chars (ellipsis);
- 5,000-line file walked via next_start_line with no gaps/overlaps;
- boundary cases (empty / one-line / start=end / EOF / beyond-EOF) behave
  per contract; total_lines accurate everywhere;
- `truncated` = Phase A codified semantics (a 200-char line clip OR a
  presentation-budget cut).

Security:
- None — path validation, traversal rejection, 2MB guard, markdown check,
  and read-only guarantees are unchanged. New tests assert read_doc
  traversal rejection and missing-file behavior. No new writes,
## 2026-09-09 — Phase C (Discovery: list_files, list_docs, find_files)

Status: IMPLEMENTED + TESTED (see evidence below; not yet part of any commit)

Changes (repository evidence — `git status` shows exactly these Phase C files):
- `src/mcquest_mcp/config.py` — added `DISCOVERY_DEFAULT_RESULTS = 100`,
  `DISCOVERY_MAX_RESULTS = 300` (Phase C default page / hard cap).
- `src/mcquest_mcp/formatting.py` — added `discovery_block()`: summary-first
  discovery-page renderer (D008/D009, Phase C) with canonical [SUMMARY]
  block (tool, scope path, ROOT, PATTERN/QUERY, COUNT, total, returned,
  offset, next_offset, has_more, truncated, collection_complete, budget,
  aggregate), exact pre-emission size accounting (finalize never pops;
  summary reflects the actual page), explicit `offset` continuation
  (page 2 never automatic), `expanded` mode (an explicit page —
  offset > 0 or max_results > default — may use the 16,000 ceiling while
  default pages use the 4,000 normal budget), 200-char per-line clip,
  [END] trailer. Mirrors `read_window_block`; no budget logic duplicated.
- `src/mcquest_mcp/tools/files.py` — `list_files` / `find_files` rewritten
  summary-first + paged: `offset` param (default 0, validated >= 0),
  default `max_results` 100 (was 200), hard cap 300; two-pass collection
  (full counting/aggregate walk for honest totals + aggregate metadata,
  then a bounded `heapq.nsmallest` page window); global `relative_path ASC`
  ordering for correct offset paging; directory + extension aggregate
  breakdown; existing filters (ignored dirs/extensions/path scoping,
  fnmatch pattern / case-insensitive substring) preserved; singleton
  file-root and traversal/missing-path behavior preserved.
- `src/mcquest_mcp/tools/docs.py` — `list_docs` rewritten identically, with
  doc-type aggregate breakdown via the existing `_classify_phase_doc`
  (IMPLEMENTATION/AUDIT/COMPLETION/VERIFICATION/OTHER); markdown-only and
  single-file-root behavior preserved.
- `src/mcquest_mcp/server.py` — discovery-tool schemas: `offset` param added
  (default 0), `max_results` default 200 -> 100 and hard cap described as
  300; descriptions updated; no parameter renamed or removed; all 17 tools
  still registered.
- `tests/test_list_files.py` (new, 22 tests),
  `tests/test_list_docs.py` (new, 17 tests),
  `tests/test_find_files.py` (new, 17 tests).

Tests (executable evidence):
- `uv run pytest tests/test_list_files.py tests/test_list_docs.py
  tests/test_find_files.py -q` -> 56 passed
- Explicit regression run
  `uv run pytest tests/test_security_readonly.py tests/test_registration.py
  tests/test_discoverability.py tests/test_output_budget.py
  tests/test_read_file.py tests/test_read_doc.py -q` -> 81 passed
- `uv run pytest` -> 175 passed, 1 skipped
  (119 + 1 baseline after Phase B unchanged; +56 new focused tests;
   no existing test was modified or regressed)

Contract verification (actual measurements, not estimates):
- default pages (150+150+10 files / 304 docs): list_files 1,706 raw chars
  (serialized 1,868); list_docs 1,656 raw (serialized 1,818); find_files
  1,673 raw (serialized 1,835) — all <= 4,000 normal budget and within the
  serialized allowance;
- explicit pages (max_results=500 clamped to 300): list_files 4,507 raw
  (serialized 4,869) — > 4,000 but <= 16,000 absolute ceiling (expanded
  mode, as designed);
- `heapq.nsmallest` page window bounds collection memory; deterministic
  global `relative_path ASC` ordering; offset=-1 rejected; offset beyond
  total returns an empty page; 601-file / 304-doc fixture totals exact.

Security:
- None — `resolve_project_path` / ignored-dir / ignored-extension /
  read-only guarantees unchanged; `offset` is a validated int (no path
  surface); no new writes, subprocesses, or git mutation. Existing
  `test_list_files_inside_root` and `test_truncate_bounds_output` remain
  green.

Known deviations:
- None.

Next:
- Phase D (search / imports / usage / evidence) after user review; see
  `03-V0.5-STATE.md`.
  subprocesses, path surfaces, or git mutation.

Known deviations:
- None.

Next:
- Phase C (discovery) after user review; see `03-V0.5-STATE.md`.
Tests (executable evidence):
- `uv run pytest tests/test_output_budget.py` -> 29 passed
- `uv run pytest` -> 82 passed, 1 skipped
  (baseline 53 passed, 1 skipped unchanged; +29 new focused tests;
  no existing test was modified or regressed)

Contract verification:
- 16,000-char ceiling proven raw AND on the serialized
  `{"content":[{"type":"text","text":out}]}` model (≤ ceiling + 500);
- 4,000-char normal budget is the target, not a cap;
- 200-char clip proven incl. a 20,000-char single line and 100k+ inputs;
- atomic item cutoff proven (items crossing the boundary by 1 char are
  rejected whole, never split);
- summary/header survives truncation;
- `[OUTPUT TRUNCATED` marker present for over-budget input
  (compat with `test_security_readonly.py::test_truncate_bounds_output`).

Security:
- None — formatter is output-only; no new writes, subprocesses, paths,
  traversal surface, or git mutation. Existing security tests green.

Known deviations:
- None.

Next:
- Phase B (reads) after user review; see `03-V0.5-STATE.md`.
## 2026-09-14 — Phase D (Search / Imports / Usage / Evidence)

Status: IMPLEMENTED + TESTED (see evidence below; not yet part of any commit)

Changes (repository evidence — `git status` shows exactly these Phase D files):
- `src/mcquest_mcp/config.py` — added `SEARCH_DEFAULT_RESULTS = 50`,
  `SEARCH_MAX_RESULTS = 500`, `SEARCH_CONTEXT_CAP_CODE = 3`,
  `SEARCH_CONTEXT_CAP_DOCS = 5` (Phase D search constants).
- `src/mcquest_mcp/formatting.py` — added `search_block()`: summary-first
  search renderer emitting the canonical [SUMMARY] block (tool, scope,
  pattern/target/symbol, total, files_affected, returned, offset,
  next_offset, has_more, truncated, collection_complete, budget) followed
  by multi-line snippets (`relative:line` header + numbered context),
  200-char per-line clip, [END] trailer, exact pre-emission size accounting
  (finalize never pops; summary reflects the actual page).
- `src/mcquest_mcp/tools/search.py` — `search_text` rewritten
  summary-first + paged: two-pass collection (full count pass for an honest
  `total`/`files_affected`, then a bounded page), explicit `offset`
  (default 0, validated >= 0), default `max_results` 50, hard cap 500, code
  context capped at 3, global `(relative_path ASC, line ASC)` ordering,
  200-char line clip.
- `src/mcquest_mcp/tools/docs.py` — `search_docs` rewritten identically
  (docs context cap 5, default `file_pattern` "*.md"); `phase_context`
  query-mode rewritten with explicit `offset` paging and default
  `max_results` 50 / `context_lines` 1; `phase`-mode structured lookup
  (IMPLEMENTATION/AUDIT grouping, phase filter honored) preserved with the
  content fallback.
- `src/mcquest_mcp/tools/imports.py` — `find_imports` / `find_usages`
  rewritten: 2MB file guard, two-pass count, explicit offset paging,
  default `max_results` 50 / hard cap 500, `(path, line)` ordering.
- `src/mcquest_mcp/tools/context.py` — `find_evidence` rewritten: merged
  code-then-docs stream (code group first, docs group second, each
  `(relative_path ASC, line ASC)`), ONE shared page budget (never a
  per-scope limit), optional phase filter (phase evidence doc-grouped),
  `counts: code=…; docs=…`, `total = code + docs`; the pre-Phase-D
  `_search_source_files` / `_search_doc_files` helpers deleted; a real bug
  fixed — removed a final global `sorted(merged)` that would have
  interleaved docs before code.
- `src/mcquest_mcp/server.py` — Phase D tool schemas (`mcquest_search`,
  `mcquest_find_imports`, `mcquest_find_usages`, `mcquest_search_docs`,
  `mcquest_phase_context`, `mcquest_find_evidence`): `offset` added
  (default 0), `max_results` default 50 / hard cap 500, `context_lines`
  caps described (0-3 code, 0-5 docs); all 17 tools still registered.
- `tests/test_find_evidence.py` — legacy pre-Phase-D assertions replaced by
  a rewrite for the summary-first / merged-stream contract.

Tests (executable evidence):
- `uv run pytest tests/test_search.py tests/test_search_docs.py
  tests/test_find_imports.py tests/test_find_usages.py
  tests/test_phase_context.py tests/test_find_evidence.py -q` -> 59 passed
- `uv run pytest` -> 229 passed, 1 skipped
  (175 + 1 baseline after Phase C, minus the 5 obsolete legacy
   find_evidence tests, plus 59 new Phase D focused tests; no existing
   test was modified or regressed)

Contract verification (actual measurements, not estimates):
- default `max_results` 50 proven for all six Phase D tools; hard cap 500
  proven (600-match fixture with caller max_results=10**6 returns exactly
  500, byte-identical to explicitly asking for 500);
- explicit offset paging proven: next_offset walks search, search_docs,
  find_imports, find_usages, phase_context query-mode, and the find_evidence
  merged stream exactly once each (no gaps/overlaps); pages 2+ never
  automatic;
- two-pass honest totals: `total`/`files_affected` exact and
  `collection_complete: true` on every page; repeated calls byte-identical;
- 200-char line clip proven on 300-char match lines in code and docs;
- context caps enforced: context_lines=99 coerced to 3 (code) / 5 (docs);
- find_evidence merged budget: 4 results across 2 code + 5 docs files
  return code-group first then docs-group under ONE shared budget;
  `scope="all"` with `phase=61` filters docs by phase; `scope="phase"` maps
  to the docs group; `total` = code + docs (never min());
- markdown-extension recognition: `*.markdown` / `*.mdown` / `*.mkd`
  searched when `file_pattern` is widened (default "*.md" kept);
- 2MB file guard proven for find_imports / find_usages (2,000,021-byte
  file skipped);
- invalid/negative offset rejected; empty query / invalid scope rejected.

Security:
- None — all Phase D changes are read-only search/paging logic; offset is a
  validated int (no path surface); no new writes, subprocesses, or git
  mutation. Existing security tests remain green.

Live MCP verification (2026-09-14, after commit e878596):
- T1/T3: `search("^def test_", tests, max_results=500, context_lines=0)` -> summary-first
  [SUMMARY] total=230, returned=161, next_offset=161, has_more=true, truncated=true,
  budget=16000/16000. The previously-measured 176-match legacy wall-of-text is gone; the
  response is bounded, paged, continuation-capable.
- T2: `offset=161` -> returned=69, has_more=false; 161+69=230, no gaps/overlaps; page 2
  reached only via explicit offset (D008).
- T4: search_docs -> total=4 returned=4 budget=4000/16000, 200-char clip applied.
- T5: find_evidence(scope="all", query="OutputBudget") -> counts code=0; docs=20, total=20,
  returned=17, next_offset=17, has_more=true, ONE shared page budget (never min()).
- T6: repeated identical query -> byte-identical first page (deterministic ordering).
- T7: search in ".git" -> total=0 (ignored-dir boundary intact).
- Regression gate re-run: tests/test_security_readonly.py test_registration.py
  test_discoverability.py test_output_budget.py test_read_file.py test_read_doc.py
  test_list_files.py test_list_docs.py test_find_files.py -q -> 137 passed.

Known deviations:
- None.

Next:
- V0.5 is CLOSED (final HEAD 0832ec8, pushed origin/main). Phase E
  (analysis: pattern_audit, diagnostics + project/git/phase) awaits user
  authorization; see `03-V0.5-STATE.md`. Do not start Phase E or F without
  approval.

---

## 2026-09-14 — V0.5 Closed (RELEASED)

Status: COMPLETED (V0.5 closed/released; documentation-only commit)

Changes:
- `00-V0.5-MASTER.md`: §3 Current Status → V0.5 CLOSED; verification block
  updated to HEAD 0832ec8, clean working tree, pushed origin/main;
  current phase/task updated.
- `03-V0.5-STATE.md`: Overall → V0.5 CLOSED; both Repository State sections
  updated (implementation base e878596; final HEAD 0832ec8 = origin/main;
  clean working tree); all four phase-ledger "Last verified commit"
  entries → 0832ec8; Next Action → V0.5 closed.
- `06-V0.5-CHANGELOG.md`: this entry + Next section updated.

Repository state:
- HEAD = 0832ec8 = origin/main ("docs: record Phase D live MCP verification
  evidence"); working tree clean.
- Phases A–D: COMPLETE (implementation at e878596; tests 229 passed /
  1 skipped; live MCP verification T1–T7 passed).
- Phases E–F: NOT STARTED.

Tests:
- `uv run pytest` → 229 passed, 1 skipped (unchanged; this is a
  documentation-only commit — no source/test changes).

Contract deviations: none.
Security changes: none.

Next:
- Phase E (Analysis: pattern_audit, diagnostics + project/git/phase) awaits
  user authorization. Do not start Phase E or Phase F without approval.