# MCQuest MCP v0.7 — Changelog

> CHANGELOG IS HISTORICAL ONLY. It is NEVER implementation authority.
> See the authority order in `00-V0.7-MASTER.md`.

---

## 2026-09-15 — Phase 0 closed (DOCUMENTATION ONLY, closeout reconciliation)

Status: **CLOSED (canonical baseline reconciled; benchmark preserved;
no code or test changes)**

Closeout (this entry; documentation-only, no new evidence collected):

- Canonical baseline: HEAD `85b84cb` = origin/main (V0.6 closeout);
  `ecd905c` is the V0.6 Phase 1 implementation predecessor (parent of
  `85b84cb`; docs-only delta). Reconciled in `00-V0.7-MASTER.md`,
  `02-V0.7-PLAN.md`, `03-V0.7-STATE.md`, `05-V0.7-DECISIONS.md`
  (DEC-004 supersedes any lingering "HEAD ecd905c" text).
- Preserved: C01–C11 runs B0–B12 + aggregates in `03-V0.7-STATE.md`
  (unchanged measurements); TEST-PLAN §D gate HELD (all 6 boxes checked).
- Recorded (DEC-005): no unnecessary shell fallback; no completeness
  errors; no MCQuest defects; no measured release-critical routing defect.
- Conclusion (DEC-006 APPROVED): Phase 1 implementation NOT justified;
  no Phase 1 scope created; observational backlog OBS-1–OBS-3 preserved
  as observations, NOT requirements.
- Verified read-only at closeout: `src`/`tests`/`Docs/V0.5`/`Docs/V0.6`/
  runtime-config diffs empty; working tree only `?? Docs/V0.7/`;
  no commit/push.

## 2026-09-15 — Phase 0 control plane created + benchmark executed
(DOCUMENTATION ONLY)

Status: **CLOSED (benchmark runs recorded; closeout reconciled; no code
or test changes)**

Changes (this entry):

- Executed read-only benchmark runs B0–B12 (C01–C11) 2026-09-15; recorded
  ledger + per-run narratives + aggregates in `03-V0.7-STATE.md`.
- Checked TEST-PLAN §D gate boxes with evidence (all 6 checked COMPLETE).
- Baseline actuals: HEAD `85b84cb` (= origin/main; docs-only child of
  `ecd905c`, reconciled in STATE); suite re-observed 305/1 (C08);
  V0.5/`src`/`tests`/`Docs/V0.6` untouched; thresholds still TBD.
- No fixes applied (observation only); no commit/push.

Changes:

- Created `Docs/V0.7/`: `00-V0.7-MASTER.md`, `01-V0.7-CONTRACT.md`,
  `02-V0.7-PLAN.md`, `03-V0.7-STATE.md`, `04-V0.7-TEST-PLAN.md`,
  `05-V0.7-DECISIONS.md`, this changelog, and
  `prompts/00-PHASE0-BENCHMARK.md`.
- Defined Phase 0 as a read-only empirical benchmark of Cline's tool
  selection (task classes C01–C11; measurement fields per CONTRACT §3).
- Marked all release thresholds TBD pending baseline results.
- Preserved baselines: closeout HEAD `85b84cb` (= origin/main; `ecd905c`
  is the V0.6 Phase 1 implementation predecessor); 305 passed, 1 skipped;
  V0.5 unchanged.
  (Closeout note: any lingering "HEAD ecd905c" phrasing in this historical
  entry is superseded by the closeout entry above + DEC-004.)

Tests:

- None authored; none required (Phase 0 benchmark contract only).

Contract deviations:

- None.

Security changes:

- None.

Next (all already DONE at closeout; restated for the historical record):

- ~~Execute `prompts/00-PHASE0-BENCHMARK.md` (read-only runs → STATE)~~ DONE.
- Do not implement MCP changes. Do not commit or push. (HELD.)
