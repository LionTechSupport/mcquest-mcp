# MCQuest MCP v0.7 — Plan

> STATUS: **V0.7 PHASE 0 CLOSED — READ-ONLY BENCHMARK RECONCILED**
>
> No implementation phase is authorized. Do not start implementation.
> Do not modify `src/`, `tests/`, `Docs/V0.5/`, `Docs/V0.6/`, or runtime
> configuration.
> Do not commit or push.

---

## Phase 0 — Establish Agent Tool-Selection Benchmark (CLOSED 2026-09-15)

Objective: establish a read-only empirical benchmark of Cline's tool
selection, using the V0.6 contract (P001–P008) and the MCQuest
tool-selection observations (Phase 0 Ledger tasks A–J, live T1–T6) as
baseline context. Procedure: `prompts/00-PHASE0-BENCHMARK.md`.

Steps:

1. Create this control plane (`Docs/V0.7/`, 7 docs + 1 prompt). DONE.
2. Verify baselines read-only: canonical HEAD `85b84cb` (= origin/main,
   V0.6 closeout; `ecd905c` is the V0.6 Phase 1 implementation
   predecessor), V0.5 untouched, V0.6 closed record intact. Recorded in
   STATE. DONE (closeout reconciliation 2026-09-15).
3. Execute benchmark task classes C01–C11 (CONTRACT §2), one or more runs
   per class, recording all measurement fields (CONTRACT §3). DONE
   (runs B0–B12, recorded in STATE).
4. Compute aggregates (MCQuest-primary rate, broad-read rate) and record
   per-run tables + aggregates in STATE. Thresholds stay TBD. DONE.
5. Record completion gate (TEST-PLAN §D). DONE — Phase 0 CLOSED
   (closeout reconciliation; no new evidence collected in closeout).

Out of scope for Phase 0: MCP changes, threshold-setting, release gating,
commits/pushes.

---

## Future Phases — NOT AUTHORIZED

No Phase 1 (or later) implementation, threshold, or release work is
authorized. Any such work requires explicit user authorization with its own
prompt and contract amendment. Do not start it from Phase 0 findings alone.
