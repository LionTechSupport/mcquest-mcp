# MCQuest MCP v0.7 — Master Document

> STATUS: **V0.7 PHASE 0 CLOSED — READ-ONLY BENCHMARK RECONCILED
> (NO IMPLEMENTATION)**
>
> Canonical baseline: HEAD `85b84cb` = origin/main (V0.6 closeout);
> `ecd905c` is the V0.6 Phase 1 implementation predecessor.
> No `src/`, `tests/`, `V0.5`, `V0.6`, or runtime-config changes.
> Thresholds remain TBD. No implementation authorized or started.
> Do not commit or push.

---

## 1. Purpose

Authoritative control plane for MCQuest MCP v0.7.

V0.6 is CLOSED (Phase 0 COMPLETE; Phase 1 COMPLETE + LIVE-VERIFIED T1–T6 PASS;
Phase 2 NOT STARTED / NOT REQUIRED; Release Gate PASS; V0.6 closeout HEAD
`85b84cb` = origin/main; `ecd905c` is the V0.6 Phase 1
implementation predecessor, child-parent: `85b84cb` docs-only closeout on
top of `ecd905c`).
V0.5 is CLOSED and unchanged.

V0.7 begins with a read-only empirical benchmark so future tool-selection or
efficiency work (if any) is gated by measured agent behavior, not assumption.

The central v0.7 Phase 0 question:

> When Cline works in this repository, does it select MCQuest as its primary
> repository-evidence interface with correct operations, complete-evidence
> awareness, clean execution boundaries, and efficient context use — and where
> it does not, what is the measured failure mode?

---

## 2. Authority Order

When documents appear to conflict, higher wins:

0. **Repository reality + executable test evidence** (always highest)
1. User's explicit instruction in the current task
2. `03-V0.7-STATE.md` (living checkpoint)
3. `01-V0.7-CONTRACT.md` (normative specification)
4. `04-V0.7-TEST-PLAN.md` (test/benchmark contract)
5. `05-V0.7-DECISIONS.md` (approved + baseline decisions)
6. `02-V0.7-PLAN.md` (plan)
7. `06-V0.7-CHANGELOG.md` (historical log)

Rules:

- Never use a lower-priority document to silently override a higher one.
- Never trust the CHANGELOG for current state — it is historical only.
- Never infer "implemented" from a plan, a prompt, a changelog entry, a
  previous Cline response, or the mere existence of a file.
- "Implemented" requires repository evidence AND relevant passing tests.
- "Measured" requires a recorded benchmark observation with tool calls,
  responses, and measurement fields — never a summary claim alone.
- If a document disagrees with repository reality, repository reality wins;
  report the discrepancy; do NOT silently reconcile the document.

---

## 3. Current Status

- V0.6 baseline preserved: closeout HEAD **`85b84cb`** (= origin/main;
  `ecd905c` is the V0.6 Phase 1 implementation predecessor).
- V0.6 test baseline preserved: **305 passed, 1 skipped** (`uv run pytest`).
- V0.5 preserved: CLOSED and untouched.
- V0.7 Phase 0: **COMPLETE 2026-09-15** — benchmark runs C01–C11
  recorded (see `03-V0.7-STATE.md`); TEST-PLAN §D gate checked.
- Release thresholds: **TBD pending baseline results** (see CONTRACT §4).
  Do not invent or gate on thresholds in Phase 0.

Current phase: **V0.7 PHASE 0 COMPLETE** (read-only benchmark recorded;
see `prompts/00-PHASE0-BENCHMARK.md` for the executed procedure).

---

## 4. Non-Negotiable Invariants (carried forward from V0.5/V0.6)

The following MUST remain true through V0.7 Phase 0:

- normal presentation budget ≤ 4000 characters
- absolute response ceiling ≤ 16000 characters
- per-line clip ≤ 200 characters
- explicit pagination only (no automatic page 2)
- deterministic ordering `(relative_path ASC, line ASC)` where applicable
- root confinement preserved
- ignored-directory / ignored-extension policy preserved
- symlink safety preserved
- read-only behavior preserved
- no writes to the target repository
- text output only
- no new security surface
- no `_meta`, no file-backed response artifacts, no `format=json`
- Phase 0 makes no `src/`, `tests/`, `Docs/V0.5/`, or runtime-config edits

---

## 5. Phase 0 Scope Discipline

Phase 0 must NOT add, and V0.7 must not authorize without explicit user
approval:

- semantic/vector search, embeddings, AI summarization
- repository indexing
- execution tools
- generated-artifact access
- broad language support
- new security surfaces
- unrelated refactoring
- MCP behavior changes of any kind
- release thresholds (marked TBD until baseline results exist)

Phase 0 produces only `Docs/V0.7/` observations and measurements.

---

## 6. Mandatory Reading (before any V0.7 benchmark run)

- this document
- `03-V0.7-STATE.md`
- `01-V0.7-CONTRACT.md` (binding premises + task classes + measurement fields)
- `04-V0.7-TEST-PLAN.md`
- `05-V0.7-DECISIONS.md`
- `02-V0.7-PLAN.md`
- the current phase prompt under `prompts/` (`00-PHASE0-BENCHMARK.md`)
- V0.6 baseline context: `Docs/V0.6/00-V0.6-MASTER.md`,
  `Docs/V0.6/01-V0.6-CONTRACT.md` (P001–P008),
  `Docs/V0.6/03-V0.6-STATE.md` (Phase 0 Ledger tasks A–J + T1–T6)
