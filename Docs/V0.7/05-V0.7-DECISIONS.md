# MCQuest MCP v0.7 — Decisions

> STATUS: **PHASE 0 RECORD**
>
> Vocabulary: APPROVED = controlling now; PROPOSED = recommendation
> requiring user authorization; RECORDED = baseline fact/classification,
> no implementation implied.

---

## DEC-001 — Phase 0 is a read-only empirical benchmark

Status: APPROVED

Decision: V0.7 Phase 0 measures Cline's tool selection with the task
classes (CONTRACT §2) and measurement fields (CONTRACT §3). It creates
only `Docs/V0.7/` content. No `src/`, `tests/`, `Docs/V0.5/`, or
runtime-configuration modification. No MCP implementation. No commit/push.

## DEC-002 — V0.6 contract + observations are baseline context

Status: RECORDED

V0.6 P001–P008 (`Docs/V0.6/01-V0.6-CONTRACT.md`), Phase 0 Ledger tasks A–J
and live T1–T6 (`Docs/V0.6/03-V0.6-STATE.md`, `Docs/V0.6/06-V0.6-CHANGELOG.md`)
are the interpretive baseline for V0.7 runs — not substitutes for them.

## DEC-003 — Thresholds are TBD pending baseline results

Status: APPROVED

No numeric release threshold is set in Phase 0. Proposing or gating on
thresholds requires a future user-authorized phase after STATE records
aggregates.

## DEC-004 — Baseline preservation

Status: APPROVED

Canonical baseline (closeout-reconciled 2026-09-15): HEAD `85b84cb` =
origin/main (V0.6 closeout). `ecd905c` is the V0.6 Phase 1 implementation
predecessor (parent of `85b84cb`; docs-only closeout delta). Suite
record 305 passed / 1 skipped, and V0.5 CLOSED-and-untouched preserved
through Phase 0. Verified read-only: `git diff --stat HEAD -- src tests`,
`-- Docs/V0.5`, `-- Docs/V0.6`, and runtime-config paths all empty;
`git status` shows only `?? Docs/V0.7/`.

## DEC-005 — Phase 0 closeout findings (RECORDED 2026-09-15)

Status: RECORDED (observation only; no implementation implied)

Preserved C01–C11 results and aggregates in `03-V0.7-STATE.md` establish:

- no unnecessary shell fallback (0 observed);
- no completeness errors (13/13 correct; 0 false "complete" claims);
- no MCQuest defects (3 caller/operation + shell-environment recoveries,
  1 freshness check FRESH with no restart needed);
- no measured release-critical routing defect.

## DEC-006 — Phase 1 NOT justified; no Phase 1 scope created

Status: APPROVED

Conclusion from Phase 0 evidence: Phase 1 implementation is NOT
justified. No Phase 1 implementation scope is created. The observational
backlog below is preserved as observations, NOT requirements; no future
work is authorized (any future phase requires explicit user
authorization with its own prompt and contract amendment).

Observational backlog (non-requirements, carried as context only):

- OBS-1: `find_usages`/`find_imports` total-0 on non-TS/JSX repos is a
  correct scope signal but was initially surprising; consider (only if
  ever authorized) discoverability wording — NOT a defect, NOT a task.
- OBS-2: nonexistent-path errors surface as plain path errors; callers
  must verify paths first — NOT a defect, NOT a task.
- OBS-3: Windows shells lack `tail`; use native equivalents — caller
  environment note, NOT a task.
