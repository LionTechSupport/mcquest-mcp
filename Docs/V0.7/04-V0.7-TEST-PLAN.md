# MCQuest MCP v0.7 — Test Plan

> STATUS: **PHASE 0 — BENCHMARK CONTRACT (NO TEST RUNS REQUIRED)**
>
> Phase 0 authors no tests and requires no test execution to pass.
> The preserved baselines below are context, not gates.

---

## A. Preserved Baselines (context only)

- V0.6 final suite (2026-09-14): `uv run pytest` →
  **305 passed, 1 skipped** (baseline was 229 passed, 1 skipped at Phase 0).
- V0.5: CLOSED; Phases A–D COMPLETE, E–F NOT STARTED.
- Phase 0 MUST NOT modify `src/`, `tests/`, or `Docs/V0.5/` and MUST NOT
  gate benchmark completion on re-running the suite.

## B. Phase 0 Benchmark Contract (normative)

For each task class C01–C11 (CONTRACT §2), minimum coverage:

| Class | Minimum run(s) | Must demonstrate |
|---|---|---|
| C01 repository search | 1 paged search | MCQuest primary; paging signals honored |
| C02 targeted file reads | 1 ranged read (+ continuation if truncated) | exact range; continuation or incomplete stated |
| C03 docs evidence | 1 search→read chain | targeted chain, not whole-doc dump |
| C04 imports/usages | 1 trace honoring TS/TSX/JS/JSX-only scope | scope-aware interpretation (incl. total-0 meaning) |
| C05 bounded/incomplete evidence | 1 deliberately truncated page + follow-up | continuation to completion or explicit incomplete stop |
| C06 cross-document evidence | 1 multi-document reconcile/diff | phase/compare or find_evidence semantics correct |
| C07 runtime/generated artifacts | 1 generated/ignored-path probe | correct boundary signal → execution workflow |
| C08 tests | 1 suite/subset reference | shell-correct classification (not a failure) |
| C09 build/typecheck | 1 build/typecheck reference | shell-correct classification (not a failure) |
| C10 process execution | 1 process-state reference | shell-correct classification (not a failure) |
| C11 stale/failed recovery | 1 simulated or historical (V0.6 T1–T6) recovery walkthrough | lifecycle classification + restart/re-verify path |

Each run records every CONTRACT §3 measurement field in STATE.
Aggregates (MCQuest-primary rate, broad-read rate) are computed, never
estimated. Thresholds: **TBD** — no pass/fail gating in Phase 0.

## C. Future Test Work — NOT AUTHORIZED

No new `tests/` files, no suite changes, no MCP behavior tests in Phase 0.

## D. Completion Gate (Phase 0 COMPLETE requires ALL)

- [x] Control plane exists (`Docs/V0.7/`, 7 docs + 1 prompt).
- [x] Canonical baseline checks recorded (HEAD `85b84cb` = origin/main,
  V0.6 closeout; `ecd905c` is the V0.6 Phase 1 implementation
  predecessor; V0.5 untouched; `src/`/`tests`/`Docs/V0.6`/runtime config
  unmodified; suite re-observed 305/1 as C08 evidence, not a gate).
  Closeout-held 2026-09-15 (no new evidence collected in closeout).
- [x] ≥1 run per class C01–C11 recorded in STATE with all §3 fields
  (runs B1–B12; B0 baseline; per-run narratives + evidence).
- [x] Aggregates computed and recorded; thresholds still marked TBD.
- [x] No `src/`, `tests/`, `Docs/V0.5/`, `Docs/V0.6/`, or runtime-config
  modifications (verified via `git diff --stat HEAD`, `git status`).
- [x] No commit or push performed.

Final status: **PHASE 0: CLOSED** — all boxes evidenced above; closeout
reconciliation held 2026-09-15 (canonical baseline `85b84cb`; predecessor
`ecd905c`); thresholds remain TBD per CONTRACT §4; no implementation
authorized, scoped, or started (DEC-006).
